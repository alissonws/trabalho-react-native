import { View } from 'react-native';
import { ThemedView } from './ThemedView';
import { ThemedText } from './ThemedText';

export function AddressViewer({}) {
  return (
    <ThemedView>
      <ThemedText>Canasvieiras, Florianópolis</ThemedText>
    </ThemedView>
  );
}
